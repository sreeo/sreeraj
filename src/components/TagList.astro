---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-1.5">
  {tags.map((tag) => (
    <a
      href={`/tags/${tag}/`}
      class="tag-pill inline-block rounded-full px-2.5 py-0.5 transition-colors"
    >
      {tag}
    </a>
  ))}
</div>
