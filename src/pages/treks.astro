---
import { getCollection } from 'astro:content';
import TagLayout from '../layouts/TagLayout.astro';

const posts = (await getCollection('blog'))
  .filter((p) => p.data.tags.some((t) => ['treks', 'trek'].includes(t)))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<TagLayout
  kicker="Section 02"
  title="Treks"
  description="Dispatches from the Himalaya — frozen lakes, summit pushes, and the trails between."
  posts={posts}
  theme="trek"
/>
